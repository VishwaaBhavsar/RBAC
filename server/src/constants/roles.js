const ROLES = Object.freeze({
  HR: 'HR',
  MANAGER: 'MANAGER',
  EMPLOYEE: 'EMPLOYEE',
});

const POLICY_STATUS = Object.freeze({
  PENDING: 'PENDING',
  APPROVED: 'APPROVED',
  REJECTED: 'REJECTED',
});

module.exports = {
  ROLES,
  POLICY_STATUS,
};

